<!DOCTYPE html>
html(lang="en")
  include ./includes/head
  body
    .wrap
      include ./includes/header
      .categories
        .categories__content
          .categories__links
            a.links(href="#") Выбор запроса
            a.links(href="#") История
          .categories__elements
            a.button(href="#") Cчет на оплату
            a.button(href="#") Договор
          .categories__back
            a(href="#") Назад
            